services:
  - type: web
    name: receipt-scanner
    env: python
    buildCommand: apt-get update && apt-get install -y tesseract-ocr && pip install -r requirements.txt
    startCommand: uvicorn src.marymount.edu.receiptscanner.app:app --host 0.0.0.0 --port $PORT